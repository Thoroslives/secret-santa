services:
  app:
    build: .
    ports: ["3001:3000"]
    environment:
      - SESSION_SECRET=${SESSION_SECRET}
      # COOKIE_SECURE unset => secure in production. Set false ONLY for tailnet HTTP, via your local .env, never here.
    volumes: ["santa_data:/data"]
volumes: { santa_data: {} }
